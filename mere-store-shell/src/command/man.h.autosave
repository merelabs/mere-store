#ifndef HELP_H
#define HELP_H

#include "../command.h"

class Man : public Command
{
    Q_OBJECT
public:
    explicit Man(QObject *parent = nullptr);
    explicit Man(QString argument,  QObject *parent = nullptr);

    bool execute() const override;
};

#endif // HELP_H
